# ========= Feishu credentials =========
# 飞书应用 App ID（必填）
APP_ID=cli_xxx
# 飞书应用 App Secret（必填）
APP_SECRET=xxxxxx
# 飞书开放网关（公网默认 https://open.feishu.cn；私有化改为你们网关）
OPEN_BASE=https://open.feishu.cn

# 事件订阅校验 Token（可选；配置后将校验）
VERIFICATION_TOKEN=your_verification_token
# 事件签名密钥 Encrypt Key（可选；建议开启签名校验）
ENCRYPT_KEY=your_encrypt_key

# ========= Service =========
# 服务监听端口与地址
PORT=3000
HOST=0.0.0.0

# ========= 文件导出配置 =========
# Excel文件输出目录
OUTPUT_DIR=/Users/tianzeyuan/Desktop
# 是否导出 CSV/Excel（true/false）
EXPORT_CSV=true
EXPORT_XLSX=true

# 仅 base 链接时的选择策略：
# - first：第一张表与第一个视图（默认）
# - by_name：按名称优先（需配合下方偏好名）
BASE_AUTO_PICK=first
# 当 BASE_AUTO_PICK=by_name 时，可指定偏好表名/视图名（示例）：
# BASE_PREFERRED_TABLE=预算明细
# BASE_PREFERRED_VIEW=全部

# ========= 日志配置 =========
# 消息日志文件路径
MESSAGES_LOG_PATH=/Users/tianzeyuan/Desktop/feishu-bitable-receiver/messages.log

# ========= 桑基图生成配置 =========
# 桑基图生成服务脚本路径（包含sankey_service_with_polling.py的目录）
SANKEY_SERVICE_PATH=/Users/tianzeyuan/Desktop/预算桑基图数据

# 桑基图HTML文件生成目录
SANKEY_OUTPUT_DIR=/Users/tianzeyuan/Desktop

# 桑基图服务监控目录
SANKEY_WATCH_DIR=/Users/tianzeyuan/Desktop

# 桑基图服务日志文件路径
SANKEY_LOG_FILE=/Users/tianzeyuan/Desktop/sankey_service.log

# 桑基图服务轮询间隔（秒）
SANKEY_POLL_INTERVAL=2

# ========= Optional (保留但不再使用的配置) =========
# SANKEY_WEBHOOK_URL=
# SANKEY_HTML_SERVER_PORT=
# SANKEY_HTML_BASE_URL=

# ========= Optional script defaults (仅脚本本地运行时使用) =========
# 若需要固定某个 base/table/view，可解开并填入；也可在命令行传参覆盖。
# APP_TOKEN=basc_xxx
# TABLE_ID=tblxxx
# VIEW_ID=vewxxx
